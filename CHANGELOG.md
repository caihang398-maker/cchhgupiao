# 版本记录

## 未发布 - 微信小程序移动端

- 新增微信云开发/云托管适配：`callContainer` 双传输、可信 OpenID 身份头校验和独立 Docker 部署入口。
- 云端支持独立数据目录；SQLite 过渡部署固定单实例并使用回滚日志，PC 端路径与 WAL 模式保持不变。
- 本地 8501 启动脚本补充 localhost 代理绕过，避免代理把健康检查误报为 503。
- 新增独立微信小程序前端和 `8512` API，PC 端 `8501` 保持不变。
- 新增今日行动清单、组合条件选股、日K分析、数据体检和模拟交易账户。
- 新增持仓做T方案、推荐池/持仓/市场一键预警及微信免密码登录。
- 新增月卡、季卡、半年卡、年卡订阅页，以及个人主体虚拟支付资格与配置检查；真实扣款默认关闭。
- 修复自定义止损规则在股票不属于推荐池时无法触发的问题。
- 小程序与 PC 端共享推荐、复盘、持仓和预警数据，并按会员隔离个人数据。

## v2.1.0 - 在线订阅测试链路

- 新增 Creem 测试结账、产品映射、会员订单和订阅记录。
- 新增独立签名回调服务，只在 `subscription.paid` 验证通过后延长会员时间。
- 回调按事件编号幂等处理，避免重复通知造成重复续期。
- 已到期会员仍可登录续费，但不能进入付费业务页面。
- 管理员可查看产品映射、订阅状态、支付回调和异常记录。
- Windows Server 增加支付回调计划任务、健康检查与发布时联动重启。
- 正式收款设有独立保护开关，默认保持关闭。

## v2.0.0 - 第二版

第二版在第一版现有业务功能上进行使用体验、数据时效和运行稳定性优化：

- 将原有大量功能标签整理为“今日决策、选股研究、持仓风控、复盘管理”四个工作区。
- 页面仅加载当前选择的功能，减少重复请求和无关计算。
- 新增“只更新行情”，盘中可快速更新市场宽度与行情状态，不必重新运行完整选股。
- 明确显示报价日期、有效报价数量、数据来源、缓存状态和过期风险。
- 实时行情失败时允许使用最近一次完整快照，但会明确标记为缓存或过期数据。
- 缩短个股查询与龙头数据缓存时间，降低盘中看到旧数据的概率。
- 增加行情状态自动化测试，覆盖实时、缓存、过期和低覆盖率场景。

## v1.0.0 - 第一版稳定基线

第一版稳定基线对应提交 `70aefc2`，也对应原服务器发布版
`v2026.07.22.2`。第一版代码、标签和安装包不会被第二版覆盖。

## 恢复第一版

只查看或重新安装第一版时：

```powershell
git fetch --tags
git switch --detach v1.0.0
```

要回到第二版：

```powershell
git switch main
git pull
```

生产服务器回退时，优先使用 GitHub 第一版 Release 中的服务器安装包，并保留
`.streamlit/secrets.toml`、`data`、`logs`、`.venv` 等服务器本地目录。
